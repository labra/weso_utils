package es.weso.utils

import org.scalatest._
import SeqUtils._
import Checker._
import jline.console.ConsoleReader

class DebuggingTest extends FunSpec with Matchers {
  

  describe("Debugging") {
    
    it("Checks debugging") {
/*      val debugger = new Debugging {
        override val withLogger = true
        override val withPrintln = false
        override val writer = System.out.println
        override val reader = new ConsoleReader().readCharacter()
      }
      debugger.debug("Hi!")
      debugger.debug("Bye") */
    }
    
  }
}